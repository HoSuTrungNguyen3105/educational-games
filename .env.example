# Socket.IO URL — backend đã deploy trên Render.
# Mặc định frontend tự lấy origin từ API_BASE (src/services/api.js).
# Chỉ cần đặt biến này nếu socket chạy ở URL khác với API.
VITE_SOCKET_URL=https://educational-games-lp4z.onrender.com